interface:
  display_name: "Zapret KVN Release"
  short_description: "Build dev first, then publish stable"
  default_prompt: "Use $zapret-kvn-release to run the dev gate first, then rebuild and publish a verified stable Zapret KVN Windows release."

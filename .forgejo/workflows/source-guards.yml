name: Windows project source guards

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: linux
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: https://data.forgejo.org/actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up Python
        uses: https://data.forgejo.org/actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip

      - name: Validate Python sources
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          python -m compileall -q xray_fluent scripts tests

      - name: Run platform-independent tests
        run: python -m unittest discover -s tests -v
